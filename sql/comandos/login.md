# Login MYSQL por linha de comando

```bash
mysql -u root -p
```

```bash
mysql -h localhost -u root -p
```
Senha:

mysql>

Selecionando o Banco

mysql> use world
Database changed

Listar todo o conteúdo do banco
mysql> select * from city;

Para sair digite:

mysql> exit